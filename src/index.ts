import type { CommitlensConfig } from "./types/config.js";

/**
 * Defines and returns a typed commitlens configuration object.
 */
export function defineConfig(config: CommitlensConfig): CommitlensConfig {
  return config;
}

export type { CommitlensConfig };
